/* ============================================
   OVERLAY — ANUNCIE COM A CLAVE
   ============================================ */

/* A ABERTURA/FECHAMENTO DO OVERLAY JÁ É GENÉRICA EM menu.js (data-action="overlay"
   data-target="anuncie"). ESTE init É O PONTO DE ENTRADA PARA O CONTEÚDO PRÓPRIO
   DO "ANUNCIE" (SUBMENU DE CATEGORIAS + CARDS GRANDES + MAPA) NA ETAPA DE DESIGN. */

export function initAnuncie() {
  const overlay = document.getElementById('overlay-anuncie');
  if (!overlay) return;
  // CONTEÚDO INTERATIVO DO "ANUNCIE" ENTRA AQUI NA ETAPA DE DESIGN.
}
