// ==========================================================
// POSTCSS.CONFIG.MJS · CONFIGURAÇÃO DO POSTCSS
// O QUE FAZ: ATIVA O PLUGIN DO TAILWIND V4 NO POSTCSS, QUE PROCESSA
//            O CSS DURANTE O BUILD DO NEXT.JS.
// QUANDO MEXER: QUASE NUNCA — CRIADO AUTOMATICAMENTE PELO CREATE-NEXT-APP.
// ==========================================================
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
